<li id="<%= dom_id(site) %>">
  <span>
    <%= link_to '×', site_path(site), method: :delete, remote: true, class: 'remove', title: 'Are you sure?', confirm: 'This action will remove your site.' %>
  </span>
  <%= link_to site_tips_path(site) do |link| %>
    <i class="icon-list-alt"></i> <b><%= site.name %></b>
    <span class="label"><%= site.tips.count %></span>
    <br>
  <% end %>
  <small class="muted"><%= site.hostname %></small>
  <small>
    <%# link_to site_tutorials_path(site) do %>
      <!--span class="label"><%= pluralize(site.tutorials.count, 'tutorials') %></span-->
    <%# end %>
  </small>
</li>
<li class="divider"></li>
